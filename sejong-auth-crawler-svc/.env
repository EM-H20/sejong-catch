PORT=8081
JWT_ISS=http://127.0.0.1:8081
JWT_AUD=sejong-catch-api

JWT_SECRET=devsecret

ACCESS_TTL_MIN=60
REFRESH_TTL_DAYS=30
TIMEOUT_SEC=5

NODE_INTERNAL_BASE_URL=http://127.0.0.1:9090

SERVICE_TOKEN=SJ_INTERNAL_TEST


# === SSO 동작 옵션 ===
SSO_USE_SESSION=1        # 세션 모드 우선 사용 (name/major 가져올 확률↑)
SSO_FALLBACK_MANUAL=1    # 세션에서 프로필 비면 Manual로 재시도(인증만 재확인)
EXPOSE_SSO=1             # 개발 중 SSO 원본(body/code) 응답에 포함 (운영은 0 권장)
DEV_BYPASS_NODE=1        # 개발 중 Node 업서트 우회 (운영은 0)


# === Crawler Scheduler 옵션 ===
CRAWLER_PAGES_PER_CATEGORY=10   # 카테고리당 순회할 페이지 수
CRAWLER_INTERVAL_SEC=600       # 반복 실행 간격(초). loop=1일 때만 사용
CRAWLER_LOOP=0                 # 1이면 무한 반복, 0이면 한 번만 실행
